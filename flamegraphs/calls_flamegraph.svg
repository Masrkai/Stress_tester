<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1400" height="342" onload="init(evt)" viewBox="0 0 1400 342" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:12px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:17px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom(true);
			zoom(target);
			if (!document.querySelector('.parent')) {
				// we have basically done a clearzoom so clear the url
				var params = get_params();
				if (params.x) delete params.x;
				if (params.y) delete params.y;
				history.replaceState(null, null, parse_params(params));
				unzoombtn.classList.add("hide");
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 12 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		var sl = t.getSubStringLength(0, txt.length);
		// check if only whitespace or if we can fit the entire string into width w
		if (/^ *$/.test(txt) || sl < w)
			return;

		// this isn't perfect, but gives a good starting point
		// and avoids calling getSubStringLength too often
		var start = Math.floor((w/sl) * txt.length);
		for (var x = start; x > 0; x = x-2) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (0 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom(dont_update_text) {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			if(!dont_update_text) update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1400.0" height="342.0" fill="url(#background)"  />
<text id="title" x="700.00" y="24" >Function Call Frequency</text>
<text id="details" x="10.00" y="325" > </text>
<text id="unzoom" x="10.00" y="24" class="hide">Reset Zoom</text>
<text id="search" x="1290.00" y="24" >Search</text>
<text id="ignorecase" x="1374.00" y="24" >ic</text>
<text id="matched" x="1290.00" y="325" > </text>
<g id="frames">
<g >
<title>SystemStressTest::performSequentialWrite (1,036,868,600 samples, 0.05%)</title><rect x="14.7" y="213" width="0.8" height="15.0" fill="rgb(253,222,53)" rx="2" ry="2" />
<text  x="17.74" y="223.5" ></text>
</g>
<g >
<title>SystemStressTest::cpuHashStressTest (1,925,627,751,232 samples, 99.53%)</title><rect x="15.6" y="245" width="1373.6" height="15.0" fill="rgb(227,102,24)" rx="2" ry="2" />
<text  x="18.62" y="255.5" >SystemStressTest::cpuHashStressTest</text>
</g>
<g >
<title>[unknown] (759,703,724 samples, 0.04%)</title><rect x="1389.4" y="229" width="0.6" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1392.41" y="239.5" ></text>
</g>
<g >
<title>[unknown] (3,978,523,184 samples, 0.21%)</title><rect x="1386.3" y="53" width="2.9" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1389.35" y="63.5" ></text>
</g>
<g >
<title>[unknown] (5,107,236,400 samples, 0.26%)</title><rect x="1385.5" y="117" width="3.7" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1388.54" y="127.5" ></text>
</g>
<g >
<title>SystemStressTest::performRandomAccess (5,857,110,639 samples, 0.30%)</title><rect x="10.1" y="213" width="4.2" height="15.0" fill="rgb(249,206,49)" rx="2" ry="2" />
<text  x="13.11" y="223.5" ></text>
</g>
<g >
<title>[unknown] (5,304,561,322 samples, 0.27%)</title><rect x="1385.4" y="165" width="3.8" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1388.40" y="175.5" ></text>
</g>
<g >
<title>[unknown] (254,913,710 samples, 0.01%)</title><rect x="1389.8" y="197" width="0.2" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1392.77" y="207.5" ></text>
</g>
<g >
<title>[unknown] (5,444,532,322 samples, 0.28%)</title><rect x="1385.3" y="181" width="3.9" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1388.30" y="191.5" ></text>
</g>
<g >
<title>[unknown] (221,542,188 samples, 0.01%)</title><rect x="1389.8" y="181" width="0.2" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1392.80" y="191.5" ></text>
</g>
<g >
<title>std::mersenne_twister_engine&lt;unsigned long, 32ul, 624ul, 397ul, 31ul, 2567483615ul, 11ul, 4294967295ul, 7ul, 2636928640ul, 15ul, 4022730752ul, 18ul, 1812433253ul&gt;::operator (268,882,473 samples, 0.01%)</title><rect x="13.5" y="181" width="0.2" height="15.0" fill="rgb(235,139,33)" rx="2" ry="2" />
<text  x="16.51" y="191.5" ></text>
</g>
<g >
<title>SystemStressTest::measureMemoryBandwidth (7,816,212,282 samples, 0.40%)</title><rect x="10.0" y="229" width="5.6" height="15.0" fill="rgb(216,52,12)" rx="2" ry="2" />
<text  x="13.02" y="239.5" ></text>
</g>
<g >
<title>all (1,934,644,259,206 samples, 100%)</title><rect x="10.0" y="293" width="1380.0" height="15.0" fill="rgb(213,39,9)" rx="2" ry="2" />
<text  x="13.00" y="303.5" ></text>
</g>
<g >
<title>unsigned long std::uniform_int_distribution&lt;unsigned long&gt;::operator (824,404,685 samples, 0.04%)</title><rect x="13.7" y="181" width="0.6" height="15.0" fill="rgb(252,216,51)" rx="2" ry="2" />
<text  x="16.70" y="191.5" ></text>
</g>
<g >
<title>__memset_avx2_unaligned_erms (153,190,236 samples, 0.01%)</title><rect x="15.5" y="213" width="0.1" height="15.0" fill="rgb(223,84,20)" rx="2" ry="2" />
<text  x="18.48" y="223.5" ></text>
</g>
<g >
<title>void std::shuffle&lt;__gnu_cxx::__normal_iterator&lt;unsigned long*, std::vector&lt;unsigned long, std::allocator&lt;unsigned long&gt; &gt; &gt;, std::mersenne_twister_engine&lt;unsigned long, 32ul, 624ul, 397ul, 31ul, 2567483615ul, 11ul, 4294967295ul, 7ul, 2636928640ul, 15ul, 4022730752ul, 18ul, 1812433253ul&gt;&amp;&gt; (3,239,549,907 samples, 0.17%)</title><rect x="12.0" y="197" width="2.3" height="15.0" fill="rgb(242,170,40)" rx="2" ry="2" />
<text  x="14.98" y="207.5" ></text>
</g>
<g >
<title>[unknown] (389,379,785 samples, 0.02%)</title><rect x="1389.7" y="213" width="0.3" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1392.68" y="223.5" ></text>
</g>
<g >
<title>[unknown] (4,265,129,297 samples, 0.22%)</title><rect x="1386.1" y="69" width="3.1" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1389.14" y="79.5" ></text>
</g>
<g >
<title>[unknown] (5,000,681,132 samples, 0.26%)</title><rect x="1385.6" y="101" width="3.6" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1388.62" y="111.5" ></text>
</g>
<g >
<title>SystemStressTest::continuousBandwidthTest (7,847,053,652 samples, 0.41%)</title><rect x="10.0" y="245" width="5.6" height="15.0" fill="rgb(212,32,7)" rx="2" ry="2" />
<text  x="13.02" y="255.5" ></text>
</g>
<g >
<title>[unknown] (5,530,693,022 samples, 0.29%)</title><rect x="1385.2" y="197" width="4.0" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1388.24" y="207.5" ></text>
</g>
<g >
<title>std::mersenne_twister_engine&lt;unsigned long, 32ul, 624ul, 397ul, 31ul, 2567483615ul, 11ul, 4294967295ul, 7ul, 2636928640ul, 15ul, 4022730752ul, 18ul, 1812433253ul&gt;::_M_gen_rand (255,788,843 samples, 0.01%)</title><rect x="14.0" y="165" width="0.2" height="15.0" fill="rgb(207,12,3)" rx="2" ry="2" />
<text  x="16.97" y="175.5" ></text>
</g>
<g >
<title>std::mersenne_twister_engine&lt;unsigned long, 32ul, 624ul, 397ul, 31ul, 2567483615ul, 11ul, 4294967295ul, 7ul, 2636928640ul, 15ul, 4022730752ul, 18ul, 1812433253ul&gt;::operator (189,726,038 samples, 0.01%)</title><rect x="14.2" y="165" width="0.1" height="15.0" fill="rgb(235,139,33)" rx="2" ry="2" />
<text  x="17.16" y="175.5" ></text>
</g>
<g >
<title>[unknown] (3,625,413,775 samples, 0.19%)</title><rect x="1386.6" y="37" width="2.6" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1389.60" y="47.5" ></text>
</g>
<g >
<title>execute_native_thread_routine (1,934,598,586,308 samples, 100.00%)</title><rect x="10.0" y="261" width="1380.0" height="15.0" fill="rgb(233,129,30)" rx="2" ry="2" />
<text  x="13.02" y="271.5" >execute_native_thread_routine</text>
</g>
<g >
<title>[unknown] (5,209,969,580 samples, 0.27%)</title><rect x="1385.5" y="149" width="3.7" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1388.47" y="159.5" ></text>
</g>
<g >
<title>SystemStressTest::memoryStressTest (1,078,255,851 samples, 0.06%)</title><rect x="1389.2" y="245" width="0.8" height="15.0" fill="rgb(206,8,1)" rx="2" ry="2" />
<text  x="1392.19" y="255.5" ></text>
</g>
<g >
<title>[unknown] (5,152,545,992 samples, 0.27%)</title><rect x="1385.5" y="133" width="3.7" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1388.51" y="143.5" ></text>
</g>
<g >
<title>[unknown] (6,146,793,162 samples, 0.32%)</title><rect x="1384.8" y="229" width="4.4" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1387.80" y="239.5" ></text>
</g>
<g >
<title>[unknown] (4,586,047,728 samples, 0.24%)</title><rect x="1385.9" y="85" width="3.3" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1388.91" y="95.5" ></text>
</g>
<g >
<title>SystemStressTes (1,934,644,259,206 samples, 100.00%)</title><rect x="10.0" y="277" width="1380.0" height="15.0" fill="rgb(248,201,48)" rx="2" ry="2" />
<text  x="13.00" y="287.5" >SystemStressTes</text>
</g>
<g >
<title>[unknown] (5,670,476,251 samples, 0.29%)</title><rect x="1385.1" y="213" width="4.1" height="15.0" fill="rgb(210,24,5)" rx="2" ry="2" />
<text  x="1388.14" y="223.5" ></text>
</g>
<g >
<title>SystemStressTest::performSequentialRead (632,843,069 samples, 0.03%)</title><rect x="14.3" y="213" width="0.4" height="15.0" fill="rgb(227,101,24)" rx="2" ry="2" />
<text  x="17.29" y="223.5" ></text>
</g>
</g>
</svg>
